{% extends "_layouts/default" %}
{% set entry = craft.entries.section('homepage').one() %}

{% block main %}
    {% include '_components/hero.twig' %}

    {% for block in entry.textBlock.all() %}
        {% include '_components/text.twig' %}
    {% endfor %}

{% endblock %}